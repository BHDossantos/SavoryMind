# Set this in the Vercel dashboard → Project → Settings → Environment Variables
# For local dev this defaults to http://localhost:8000 (no change needed)
NEXT_PUBLIC_API_URL=https://api.savorymind.net
